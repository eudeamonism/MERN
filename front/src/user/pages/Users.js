import React from 'react'
import UsersList from '../components/UsersList'
function Users() {
    const USERS = [{
    
    }];

//we pass items property with a handler that will contain DB. This prop will be extracted down the totem pole.
  return (
      <UsersList items={ } />
  )
}

export default Users